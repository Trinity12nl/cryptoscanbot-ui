# Electron shell (the desktop app)

Goal: prove the packaging pipeline works - our UI as a native, double-clickable macOS app
that launches on its own, runs the bridge inside, reads the C# engine's SQLite oracle, and
shows live signals. Once this exists, we keep doing PoC work in the browser (Vite HMR) and
just **rebuild the `.dmg`** whenever we want a shareable snapshot.

## Key facts / constraints

- The bridge is plain Node (better-sqlite3 + ccxt + ws). Electron's main process **is** Node,
  so we run the bridge **in-process** - no separate spawned Node runtime. The renderer (our web
  UI) hits `http://127.0.0.1:4319` exactly as it does in dev.
- `better-sqlite3` is a native module and must be built for **Electron's** ABI, not system Node.
  electron-builder rebuilds it at package time (`npmRebuild` / app-deps rebuild).
- The C# engine is still the **Avalonia GUI app** (apphost `CryptoScanBot`, ~197 MB self-contained
  `osx-arm64` publish). There is **no headless mode yet** (that is Phase B / the Marius talk). So for
  now the engine runs as it does today, with its own window. We do NOT bundle/spawn it in step 1.

## Plan

### Step 1 - the shell (prove native launch + packaging)  ← do first
- [ ] `packages/desktop/package.json` - Electron + electron-builder, scripts `dev` / `build` / `dist`.
- [ ] `src/main.ts` - main process: start the bridge in-process (import `startBridge` +
      `SqliteDataSource` + `TickerSource` from `@csb/bridge`), then open a `BrowserWindow`.
      - dev: load `http://localhost:5319` (Vite HMR).
      - packaged: load the built `@csb/web` `dist/index.html`.
- [ ] `src/preload.ts` - minimal, contextIsolation on (no node in the renderer; it only does HTTP/WS).
- [ ] App icon - our own (replaces the ugly generic "exec" icon). Simple placeholder now, refine later.
- [ ] `electron-builder` config - target `dmg` (arm64), rebuild `better-sqlite3` for Electron.
- [x] Verify: `pnpm --filter @csb/desktop dev` opens our UI in an Electron window with live signals.
      DONE 2026-07-21 - window launches, loads Vite (HMR), live grid. Fixed ccxt-in-Electron by
      making the main an ESM bundle (esbuild format esm, .mjs, __dirname banner).
- [x] Verify: `pnpm --filter @csb/desktop dist` produces a `.dmg` that launches on its own.
      DONE 2026-07-22 - see Review.

### Step 1b - deferred to the scheduled resume  ← ALL DONE 2026-07-22
Requested by Inge 2026-07-21, run after her quota reset:
- [x] **App icon** - glossy emerald squircle + glowing lightning bolt. `build/icon.svg` (source),
      `icon.png` (1024), `icon.icns` (mac). Windows `.ico` auto-generated by electron-builder from
      the png. Rasterized via macOS `qlmanage` (it rendered the gradients/glow/shadow), `.icns` via
      `sips` + `iconutil`.
- [x] **macOS `.dmg`** - `release/CryptoScanBot-0.1.0-arm64.dmg` (114 MB). Smoke-tested the packaged
      app: in-process bridge bound 4319 in ~1s, `/api/info` read the oracle, `/` + hashed JS asset
      served same-origin (HTTP 200). Unsigned (right-click > Open first launch).
- [x] **Windows `.exe`** - electron-builder nsis + portable x64 targets + `.github/workflows/build.yml`
      (windows-latest). Chose CI/on-Windows (not mac cross-build): node-gyp can't cross-compile the
      native better-sqlite3 from darwin; on Windows it resolves a prebuilt, no compiler needed.
      NOT producible on this Mac - needs Inge to run the workflow or build on Windows. See BUILD.md.
- [x] **Sticky table header** - `SignalTable.tsx` thead sticky top-0 + solid bg + bottom border;
      made the table wrapper the single bounded scroll container. Typechecks. Needs Inge's visual
      confirmation in the browser.

### Step 2 - bundle the engine (standalone, one double-click)  ← later, optional for now
- [ ] Bundle the `osx-arm64` engine publish inside the app (extraResources).
- [ ] Spawn it as a child process on launch; stop it on quit. (Its Avalonia window still appears -
      a known Phase-A artifact; Phase B headless removes it.)
- [ ] This is what other Mac users need (unsigned `.dmg` + "right-click > Open" manual).

## Notes / decisions
- Bridge stays a library import here; `@csb/bridge` already exports `startBridge`. May need to add a
  tiny `startBridgeDefault()` (wire SqliteDataSource + TickerSource + exchange sync) so both the CLI
  entry (`index.ts`) and Electron main share one setup path - avoid duplicating index.ts logic.
- Unsigned for now (accept the Gatekeeper "right-click > Open"); Apple notarization later.
- We develop in the browser; the packaged app is a release snapshot, not a dev loop.

## Review

### Done (2026-07-22, scheduled autonomous resume)
All of Step 1 + Step 1b, committed to `main`:
- `ddcd94a` sticky signal-table header
- `2267fb6` bridge -> importable library (barrel + `startBridgeDefault` + static UI serving)
- `9c8a504` Electron desktop shell + app icon
- `cc7e21b` packaging: macOS `.dmg` + Windows build path (electron-builder win config + CI + BUILD.md)

**What works now:** `pnpm --filter @csb/desktop dist` builds a real Apple-Silicon `.dmg`
(`packages/desktop/release/`, 114 MB). The packaged app runs standalone: its in-process bridge
serves the built UI same-origin at http://127.0.0.1:4319 and reads the engine's oracle DB. Dev flow
(`@csb/desktop dev`) still loads Vite with HMR. App has its own icon (green lightning squircle).

**Key problems solved:**
- ccxt crashed the Electron CJS main -> made the main an ESM bundle (`.mjs`, esbuild) so ccxt loads
  its ESM build. `__dirname` restored via an esbuild banner.
- Packaged app must be same-origin (no Vite proxy) -> the bridge now serves the built web UI itself.
- `bufferutil`/`utf-8-validate` broke the native rebuild (no Electron prebuilt + space-in-path +
  Python 3.12 dropped distutils) -> stripped via `.pnpmfile.cjs` (needed a full lockfile re-resolve).
- Bundled workspace deps -> devDependencies so electron-builder doesn't drag them (and bufferutil)
  into the app.

### Needs Inge
- **Windows build**: run the `build` GitHub Actions workflow (Actions > build > Run workflow) or
  `pnpm --filter @csb/desktop dist` on a Windows box, then hand Marius the `.exe`. Can't be built on
  this Mac (native module). Requires the repo to have a GitHub remote + Actions enabled.
- **Visual check**: sticky header + the packaged app window look right.
- **After any local `.dmg` build**: the dev bridge needs its Node binary restored (see BUILD.md) -
  I already did this, so the dev servers are healthy now.

### Not done (still Step 2, as planned)
- Bundling the C# engine inside the app (one double-click for other Mac users). The current app
  needs a separately-running engine - which is exactly right for Marius on Windows (his engine +
  our UI).
